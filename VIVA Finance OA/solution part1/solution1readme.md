To run this program, cd into the folder containing both the csv and json files. 

To execute the script, simply call 
```
python3 solution.py payments.csv db.json applied.json
```
in that exact order.
